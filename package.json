{
  "name": "juego-root",
  "version": "1.0.0",
  "description": "Proyecto para juegos locales y vidriera",
  "private": true,
  "scripts": {
    "install-all": "npm install --legacy-peer-deps && npm install --prefix functions --legacy-peer-deps && npm install --prefix vidriera --legacy-peer-deps",
    "start-emulators": "firebase emulators:start",
    "dev:vidriera": "npm run dev --prefix vidriera"
  },
  "devDependencies": {
    "firebase-tools": "^13.15.0"
  }
}
